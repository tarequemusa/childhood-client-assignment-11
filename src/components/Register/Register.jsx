import useTitle from "../../Hooks/useTitle";


const Register = () => {
    useTitle('Register');
    return (
        <div>
            <h2>This is Register</h2>
        </div>
    );
};

export default Register;