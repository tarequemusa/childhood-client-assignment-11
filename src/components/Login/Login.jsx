import useTitle from "../../Hooks/useTitle";


const Login = () => {
    useTitle('Login')
    return (
        <div>
            <h2>This is Login</h2>
        </div>
    );
};

export default Login;